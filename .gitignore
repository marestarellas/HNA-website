# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the template must be committed — it is the only place the required
# variables are documented for someone cloning this repo.
!.env.example

# vercel
.vercel

# cloudflare / opennext
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts

# remotion render output
/out/

# agent tooling — machine-local settings (shared config lives in .claude/settings.json)
.claude/settings.local.json

# typescript
*.tsbuildinfo
next-env.d.ts
